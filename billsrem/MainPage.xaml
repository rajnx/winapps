<Page
    x:Class="BillsReminder.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BillsReminder"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="600" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="{StaticResource AppName}" VerticalAlignment="Center"  HorizontalAlignment="Center" FontSize="150" FontWeight="Light"/>
        <StackPanel Grid.Row="1" Margin="20" Orientation="Vertical">
            <Button Content="Login using MSA" Margin="20,20,0,0" HorizontalAlignment="Center"  Foreground="Black"/>
            <Button Content="Skip Login" Margin="20,20,0,0" HorizontalAlignment="Center" Foreground="Black" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Page>
